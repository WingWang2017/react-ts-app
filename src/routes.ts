
import Login from './pages/login';
import School from './pages/login/school';

const routes: any[] = [
  { path: '/login', component: Login },
  { path: '/login/school', component: School },
];

export default routes;
